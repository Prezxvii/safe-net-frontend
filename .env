# =================================================================
# FRONTEND CONFIG (safe-net-app/.env)
# These variables MUST start with REACT_APP_ to be accessible in the client. 
# They are NOT secrets, as they are exposed in the browser build.
# =================================================================

# The URL of your local backend server endpoint for classification
# The React app will call this URL:
REACT_APP_CLASSIFY_API_URL="http://localhost:4000/api/classify"